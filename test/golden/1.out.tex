\documentclass{article}\title{Generated Anki Notecards}\author{siddharthist}\begin{document}\maketitle{}\begin{note}\begin{field}Define the term Limit of a function $f:A\subseteq \C\to\C$.\end{field}\begin{field}
	Let $f:A\subseteq\C\to\C$ and suppose that $a\in\C$ is a limit point of $A$.
  The limit of $f(x)$ as $x$ approaches $a$ is $L\in\C$ if $\forall \epsilon >
  0\in \R$, there exists $\delta>0\in \R$ such that $\forall x\in A$ if
  $0<|x-a|<\delta$, then $|f(x)-L|<\epsilon$.

  We give this the notation
  \begin{equation*}
    \lim_{x\to a} f(x)=L
  \end{equation*}
\end{field}\end{note}\begin{note}\begin{field}\end{field}\begin{field}\end{field}\end{note}\end{document}